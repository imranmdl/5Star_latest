/**
 * Deployment settings.
 *
 * THIS IS THE ONLY FILE YOU NEED TO EDIT TO DEPLOY.
 *
 * Set the address of your backend API below. It must end in /api/v1.
 *
 * If the storefront and the API are served from the SAME host, a path is enough
 * and no CORS configuration is needed:
 *
 *     window.SPICE_API_BASE = '/api/v1';
 *
 * If they are on different hosts or ports — which is the usual case during
 * development — give the full address, and configure CORS on the backend to
 * allow the storefront's origin:
 *
 *     window.SPICE_API_BASE = 'http://localhost:8081/api/v1';
 */
window.SPICE_API_BASE = 'https://chocolate-deer-714353.hostingersite.com/spice-api/backend/public/api/v1';
